# Minimal ALSA config for statically-linked musl builds.
# Only references ALSA built-in plugins (plug/dmix/hw) so no external
# PipeWire/PulseAudio plugins need to be dlopen'd at runtime.
pcm.!default {
    type plug
    slave.pcm "dmix:0,0"
}

ctl.!default {
    type hw
    card 0
}
